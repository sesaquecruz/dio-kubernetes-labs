server:
  port: 80
  servlet:
    context-path: "/"

spring:
  application:
    name: app
  datasource:
    url: jdbc:postgresql://database-connection/app
    username: admin
    password: admin123
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect